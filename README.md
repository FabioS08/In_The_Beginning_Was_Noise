# DDPM


After cloning the repo, go in the directory and


(LOOK FOR environment.yml ->> conda env create -f environment.yml)

- conda create -n $(basename $PWD) python=3.10

Then:

- conda activate $(basename $PWD)


when finished:

- conda deactivate
- conda remove -n myproject --all
